const fs = require("fs");

// const buf = Buffer.alloc(10);
// buf.write("some new data");
// console.log(buf.toString());

// const buf2 = Buffer.from("hello from the node js");
// console.log(buf2.toJSON());

// fs.readFile("./change.txt", (err, data) => {
//     if (err) {
//         console.log(err);
//     }
//     console.log(data.toString());
// });

// const buf = Buffer.from("some new data");
// console.log(buf.includes("some"));
// console.log(Buffer.isEncoding("data"));
// console.log(buf.includes("new"));

// const bufferData = Buffer.from("hi");
// const bufferNewData = Buffer.from("hi");
// console.log(Buffer.compare(bufferData, bufferNewData)); // -> 0, -1
// console.log(Buffer.compare(bufferData, buf));

// console.log(bufferData.equals(bufferNewData));

// const buf = Buffer.from("some new buffer string data");
// console.log(buf);
// console.log(buf.subarray(0, 4).toString());
